import { Slide } from "./Slide";
import { Output } from "../IO/OutputWriter";

export class OutputParser {

    parse(slide: Slide): Output {
        return {
            id1: 0,
            id2: 0
        }
    }

}